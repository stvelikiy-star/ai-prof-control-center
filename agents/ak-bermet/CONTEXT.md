# AK BERMET Agent Context

Project: AK BERMET SPA & WELLNESS.

Current task target is the isolated pilot repository:
`/home/agent/projects/ak-bermet-agent-pilot`

Rules:

- Work only inside the target repository supplied by the validated task.
- Modify only approved Scope files.
- Do not access `.env`, credentials, secrets or production data.
- Do not modify dependencies, lock files, migrations or configuration unless explicitly approved.
- Do not commit, merge, push or deploy.
- Preserve existing architecture and terminology.
- Prefer deterministic, evidence-based changes.
- The original repository `/home/agent/projects/ak-bermet` must remain unchanged.
